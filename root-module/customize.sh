#!/system/bin/sh
MODDIR=${0%/*}

ui_print "Causentry STEP 1 skeleton installed."
ui_print "Provider detection, APK bootstrap, and daemon lifecycle arrive in later steps."
ui_print "Module directory: ${MODDIR}"

exit 0
