# Legacy Xposed Hook Reference

These files are kept only as porting reference for the future bundled ART hook
runtime. They are not under `payload-src/src`, are not compiled into
`Causentry.apk`, and must not be wired back through Vector/LSPosed fallback paths.
