package defpackage;

import java.util.Iterator;
import java.util.concurrent.ConcurrentHashMap;

/* JADX INFO: loaded from: classes4.dex */
public final class mapPoints {
    public final ConcurrentHashMap<String, isUsingRotationDegrees> TuitionPaymentFragmentspecialinlinedviewModeldefault2 = new ConcurrentHashMap<>();
    public final ConcurrentHashMap<String, FileTransformFactory> b = new ConcurrentHashMap<>();

    public final void TuitionPaymentFragmentspecialinlinedviewModeldefault2(Iterable<isLenovoTablet> iterable) {
        Iterator<isLenovoTablet> it = iterable.iterator();
        while (it.hasNext()) {
            TuitionPaymentFragmentbindingInflater1(it.next());
        }
    }

    private final void TuitionPaymentFragmentbindingInflater1(isLenovoTablet islenovotablet) {
        Iterator<T> it = islenovotablet.b.iterator();
        if (it.hasNext()) {
            mapPoint mappoint = ((getRotatedCropRect) it.next()).TuitionPaymentFragmentspecialinlinedviewModeldefault3;
            throw null;
        }
    }
}
