package com.v7878.unsafe;

/** Minimal library BuildConfig replacement for the vendored PanamaPort sources. */
public final class BuildConfig {
    public static final boolean DEBUG = false;

    private BuildConfig() {}
}
